/* import fetch from 'node-fetch';
const API = 'https://api.escuelajs.co/api/v1';

function fetchData(urlApi) {
    const res = fetch(urlApi);
    const data = res.json();
    return data;
}

function myData() {
    const arrayData = () => {
        for(datos of dataArray) {
            yield datos;
        }
    }

    console.log();
}

myData(fetchData(`${API}/products`)); */